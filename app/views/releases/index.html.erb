<h1>BOSH Release Index</h1>
<br />
<table class="table table-bordered table-striped">
  <tr>
    <th>Name</th>
	<th>Rating</th>
	<th>Downloads
    <th>User</th>
	<th>Latest Version</th>
  </tr>

<% @releases.each do |release| %>
  <tr>
    <td><%= link_to release.name, release %></td>
    <td><%= release.rating %>
	<td><%= release.versions.last.downloads %>
	<td><%= release.user.username if release.user %></td>
	<td><a href="/releases/<%= release.id %>/version/<%= release.versions.last.id %>/download">v<%= release.versions.last.id %> - Download</a></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Release', new_release_path, :class => "btn" %>
